/** Plot area height passed to Recharts `ResponsiveContainer` (keep skeleton in sync). */
export const SCORE_CHART_PLOT_HEIGHT_PX = 280;

/** Approximate card height: title + subtitle + legend + plot + padding. */
export const SCORE_CHART_CARD_SKELETON_HEIGHT_PX = SCORE_CHART_PLOT_HEIGHT_PX + 88;
